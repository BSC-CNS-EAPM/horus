Bootstrap: docker
From: sickcodes/docker-osx:monterey

%environment
    export PATH="/opt/conda/bin:${PATH}"

%files
    ./Devtools/Environment/conda_horus.yaml Devtools/Environment/conda_horus.yaml
    ./package.json package.json

%post
    # Install dependencies
    dnf install -y gcc gobject-introspection-devel cairo-gobject-devel pkg-config python3-devel gtk3-devel
    dnf install -y rpmdevtools rpmlint

    # Install miniconda
    curl -LO https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
    bash Miniconda3-latest-Linux-x86_64.sh -b -p /opt/conda
    rm Miniconda3-latest-Linux-x86_64.sh
    echo "source /opt/conda/etc/profile.d/conda.sh" >> ~/.bashrc
    echo "conda activate base" >> ~/.bashrc

    # Activate conda
    source /opt/conda/etc/profile.d/conda.sh
    conda init

    # Install nodejs 18
    curl -sL https://rpm.nodesource.com/setup_18.x | bash -
    dnf install -y nodejs

    # Install requirements
    npm run requirements

%runscript
    # Activate conda environment
    source /opt/conda/etc/profile.d/conda.sh
    conda activate horus
    npm run build
    npm run distribute
